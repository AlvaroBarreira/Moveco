import Axios from 'axios';
import config from '../Config/config';
import { successAlert } from './AlertsServices';
import { errorAlert } from './AlertsServices';

export const UseSendForm = async () => {
    // try {
    //     const res = await Axios.post(
    //         `${config.REACT_APP_SERVER}/api/consultas`,
    //         {
                
    //         }
    //     );
    //     const text = 'Su consulta ha sido enviada satisfactoriamente'
    //     successAlert(text);

    //     return res.data;

    // } catch (error) {
    //     const text = "Hubo un error al intentar enviar su consulta, intentelo de nuevo mas tarde";
    //     errorAlert(text)
    // }
}
