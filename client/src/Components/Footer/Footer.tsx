import React from 'react';

interface FooterProps {
    values: object;
}

export const Footer: React.FC<FooterProps> = ({ values }) => {
    return (
        <>
            <h1>Footer</h1>
        </>
    );
};

